spring:
  profiles:
    active: local

springdoc:
  swagger-ui:
    path: /swagger  # Swagger UI ?? ??? /swagger? ??
    display-request-duration: true  # ?? ????? UI? ??
  api-docs:
    enabled: true  # OpenAPI Docs (JSON) ???
    path: /v3/api-docs  # OpenAPI Docs ?? ??
  cors:
    enabled: true  # CORS ?? ??

---

spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306
    username: sajang_user
    password: sajang_password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQLDialect